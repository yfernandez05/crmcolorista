<template>
    <form-plan-estudio cardTitle="Registrar Plan de Estudio" @saveData="saveData">
    </form-plan-estudio>
</template>

<script>
    import FormPlanEstudio from './_FormPlanEstudio';

    export default {
        methods: {
            saveData(event) {
                showPreloader();
                console.log('save data', event);
                let vm = this;
                axios.post(`${appApiUrl}/planestudio`, event)
                    .then(function (response) {
                        hidePreloader();
                        let result = response.data;

                        if (result.status) {
                            successMessage(result.message, appName);

                            vm.$router.push({
                                name: 'spa.planestudio'
                            });
                        } else
                            errorMessage(result.message, appName);
                    })
                    .catch(function (error) {
                        hidePreloader();
                        errorMessage(appErrorMessage, appName);
                        console.log(error);
                    })
            }
        },
        components: {
            FormPlanEstudio
        }
    }

</script>
